import 'package:flutter/material.dart';

class AppColors {
  // App colors
  static const primary = Color(0xFFFFFFFF);
  static const onPrimary = Color(0xFFFB7F6B);
  static const onError = Color(0xFFFF0000);

  static const backgroundColor = Color(0xFFFFFFFF);
  static const textColor = Color(0xFF000000);
  static const appBarColor = Color(0xFFFB7F6B);
  static const searchBarColor = Color(0xFFFB7F6B);
  static const messageColor = Color(0xFFFB7F6B);
  static const white = Colors.white;
  static const appBarTitleColor = Colors.white;
}
